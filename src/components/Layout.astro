---
const { title, description, canonical } = Astro.props;
const url = canonical ?? Astro.url;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />

    <title>{title}</title>
    {description ? <meta name="description" content={description} /> : null}
    <link rel="canonical" href={url.toString()} />

    <meta name="generator" content={Astro.generator} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <!-- Time.is uses Montserrat for most text, and a custom clock font (TimeTraveler) for the big digits.
         We can match the feel with Montserrat + a heavy Montserrat weight for the hero number. -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800;900&display=swap" rel="stylesheet" />

    <link rel="stylesheet" href="/styles/global.css" />
  </head>
  <body>
    <header class="site">
      <div class="container">
        <div class="row">
          <div>
            <a href="/" style="font-weight:800; color: var(--text); letter-spacing: .01em;">CodeToCall</a>
            <div class="small">International dialing, made simple</div>
          </div>
          <nav>
            <a href="/dial-builder/">Dial Builder</a>
            <a href="/about-data/">About the data</a>
          </nav>
        </div>
      </div>
    </header>

    <main class="container">
      <slot />
    </main>

    <footer>
      <div class="container">
        <div class="small">
          © {new Date().getFullYear()} CodeToCall · International dialing helper · <a href="/about-data/">Data & methodology</a>
        </div>
      </div>
    </footer>
  </body>
</html>
