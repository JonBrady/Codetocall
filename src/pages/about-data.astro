---
import Layout from '../components/Layout.astro';

const title = 'About — CodeToCall';
const description = 'How CodeToCall formats international numbers, what data is used, and known limitations.';
---

<Layout title={title} description={description}>
  <h1 style="margin: 22px 0 6px;">About the data</h1>
  <div class="btnRow" style="margin: 10px 0 0;">
    <a class="btn" href="/" aria-label="Back to homepage">← Back to home</a>
  </div>
  <div class="card" style="margin-top: 14px;">
    <p class="small">
      CodeToCall uses a curated, versioned dataset (stored as JSON) so the site stays fast, cheap to run, and consistent.
    </p>

    <h2>What’s included</h2>
    <ul>
      <li>Country name + ISO2 code</li>
      <li>Country calling code (digits only)</li>
      <li>Trunk prefix (if commonly used)</li>
      <li>Example formats + simple notes</li>
      <li>Origin exit codes (IDD prefix) for legacy formatting</li>
    </ul>

    <h2>Limitations</h2>
    <ul>
      <li>This is a dialing helper, not a perfect global phone-number authority.</li>
      <li>Some countries have nuanced rules that require special-casing.</li>
      <li>Some calling codes are shared (e.g., NANP +1), so destination detection can be ambiguous.</li>
    </ul>

    <h2>Quality checks</h2>
    <p class="small">
      We run automated checks on the dataset so updates don’t accidentally break formatting.
    </p>
  </div>
</Layout>
